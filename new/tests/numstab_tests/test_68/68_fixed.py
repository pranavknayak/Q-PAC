import numpy as np

def fidelity(estimate, reference):
    # Compute fidelity with respect to some baseline
    pass

fid3 = fidelity(est3, ref)
fid4 = fidelity(est4, ref)
fid5 = fidelity(est5, ref)
avg_fid = np.mean([fid1, fid2, fid3, fid4, fid5])
